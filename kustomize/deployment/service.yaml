apiVersion: v1
kind: Service
metadata:
  name: odootools
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: http
  selector:
    app.kubernetes.io/name: odootools
    app.kubernetes.io/component: web
